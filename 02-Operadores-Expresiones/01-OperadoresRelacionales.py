"""
En una lista llamada 'expresiones' encontramos diferentes operaciones relacionales, ¿podrias determinar mentalmente el
resultado de cada expresion y almacenarlo en una nueva lista llamada resultados? Esta lista debe contener unicamente
valores logicos TRUE y FALSE . Por ejemplo esta lista: expresiones = [1 == 2, 1 < 0]
La respuesta que se espera que escribas es:
                                            resultados = [True, False]
Correspondiente True al resultado de 1 == 1 y False al de 1 < 0, es decir, los resultados deber concordar con la
posición de las expresiones, siendo resultados[0] la respuesta a la expresion expresiones[0]
La lista de expresiones que debes calcular mentalmente es la siguiente:
    expresiones = [False == True, 10 >= 2 * 4, 33 / 3 == 11, True > False, True * 5]
"""

expresiones = [False == True, 10 >= 2 * 4, 33 / 3 == 11, True > False, True * 5 == 2.5 * 2]

# completa el ejercicio
resultados = [False, True, True, True, True]
